# Local development (default — talks to backend on port 8000)
# VITE_API_URL=http://localhost:8000

# Production — set in Vercel → Project → Settings → Environment Variables
# VITE_API_URL=https://YOUR-CLOUD-RUN-URL.run.app
